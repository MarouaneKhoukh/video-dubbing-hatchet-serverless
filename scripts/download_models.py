#!/usr/bin/env python3
"""Pre-download pipeline models to data/models — or use ``python -m models.download_models``."""

from models.download_models import main

if __name__ == "__main__":
    main()
